from setuptools import setup, find_packages

setup(
    name="trass-recomendation",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        "PyQt6>=6.0.0",
    ],
    python_requires=">=3.8",
) 